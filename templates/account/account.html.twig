{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!{% endblock %}

{% block body %}

<div class="example-wrapper">
    {% if app.user is null %}
        <a class="nav-link link-primary ms-2" href="{{ path('app_contact') }}">Contact</a>
    {% else %}
    <h1>Bonjour et bienvenue dans votre espace, {{ app.user.firstname }} {{ app.user.lastname }} !</h1>
    {% endif %}
</div>
{% endblock %}
